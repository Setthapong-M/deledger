# Deledger context

Read `AGENTS.md`, then follow `project-context/smart_router.yml` for the task. Shared standards live in `project-context/`; load matching routes rather than the whole folder. Follow directory-specific agent instructions for files being changed.
