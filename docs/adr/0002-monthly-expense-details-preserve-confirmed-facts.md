# Monthly expense details preserve confirmed facts

A Monthly Expense Detail snapshots the Monthly Recurring Expense name, type, and amount at confirmation instead of reading those fields live from Monthly Expense Setup. This duplicates a small amount of data but ensures later setup edits cannot rewrite what the User previously confirmed; changing a confirmed fact requires cancellation and a new confirmation.
